/*
Example Application Mapper

export class ApplicationExampleMapper {
    static toExampleCommand(data: any): ExampleCommand {
        return new ExampleCommand(
            data.id,
            data.name,
            data.description
        )
    }
    static toEntity(command: any): Entity {
        return new Entity(
            command.id,
            command.name,
            command.description
        )
    }
}
*/